export const container = ['40em', '52em', '64em', '80em'];

container.sm = container[0];
container.md = container[1];
container.lg = container[2];
container.xl = container[3];

export default {
  container
}